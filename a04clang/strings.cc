// TODO: git pull llvm-mos and check if there is version 20.20.02+ or wait for a new release

// #include <charset.h>
// #include <atari.h>
// #include <stdio.h>
// #include <string.h>

// char[] atascii = "ATASCII"_a;
// char[] international = "Internatiónal"_i;

// char[] screen = "Internal"_av;
// char[] screenReverse = "Internal reverse"_arv;

// char[] screenInternational = "Internal internatiónal"_iv;
// char[] screenInternationalReverse = "Internal internatiónal reverse"_irv;

// int main()
// {
	// printf("\n\n\n\n");
    // printf(atascii);
	// printf("\n");
	// printf(international);
	// memcpy(OS.savmsc, screen, strlen(screen));
	// memcpy(OS.savmsc+40, screenReverse, strlen(screenReverse));
	// memcpy(OS.savmsc+80, screenInternational, strlen(screenInternational));
	// memcpy(OS.savmsc+120, screenInternationalReverse, strlen(screenInternationalReverse));

    // OS.ch = KEY_NONE;
    // while (OS.ch == KEY_NONE)
        // asm volatile("");

    // return 0;
// }